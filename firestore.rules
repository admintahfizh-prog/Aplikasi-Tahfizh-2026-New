rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Multi-device school portal rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
